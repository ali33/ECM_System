namespace Ecm.CaptureDomain
{
    /// <summary>
    /// Represent the object type that workfow apply
    /// </summary>
    public enum WorkflowObjectType
    {
        /// <summary>
        /// Workflow is applied on documents in the system
        /// </summary>
        Document,

        /// <summary>
        /// Workflow is applied on batches in the system
        /// </summary>
        Batch
    }
}
